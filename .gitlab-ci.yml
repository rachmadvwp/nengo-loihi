before_script:
  - pip3 install --user --upgrade pip
  - pip install --user --upgrade flake8 pylint pytest
  - pip install --user --upgrade -e .

static_checks:
  script:
    - flake8 nengo_loihi && pylint nengo_loihi

tests:
  script:
    - pytest nengo_loihi
